# Archive

Esta carpeta se reserva para snapshots, decisiones viejas, notas temporales o material historico.

## Regla

Lo que este aqui no gobierna automaticamente el presente.
Si una decision archivada vuelve a ser vigente, debe moverse o resumirse en `knowledge/`, `rules/` o `policies/`.
